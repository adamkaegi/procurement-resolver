# Generated by qwen2.5:7b-instruct, then human-reviewed and corrected --
# the unedited model output is archived beside this file
# (mapping.ollama_unedited.yaml) and the review's corrections are ADR'd in
# docs/DECISIONS.md ("Generated, then reviewed"). Corrections: ocid mapped
# (schema requires it; model abstained), amount gains the negative-amendment
# fallback column, description mapped (model missed a populated column),
# classification order fixed (unspsc before gsin-nibs), id moved to the
# reliably-populated referenceNumber.
source_id: canadabuys_contract_history
jurisdiction: federal
mapping_version: phase-6-llm-reviewed
mappings:
  - canonical: ocid
    source_fields: [solicitationNumber-numeroSollicitation, referenceNumber-numeroReference]
    transform: first_present
  - canonical: id
    source_field: referenceNumber-numeroReference
    transform: direct
  - canonical: date
    source_fields: [contractAwardDate-dateAttributionContrat, publicationDate-datePublication]
    transform: parse_date
  - canonical: buyer.name
    source_field: contractingEntityName-nomEntitContractante-eng
    transform: direct
  - canonical: awards[].suppliers[].name
    source_fields: [supplierLegalName-nomLegalFournisseur-eng, supplierOperatingName-nomCommercialFournisseur-eng]
    transform: first_present
  - canonical: awards[].value.amount
    source_fields: [contractAmount-montantContrat, totalContractValue-valeurTotaleContrat]
    transform: parse_nonnegative_currency_with_total_fallback
  - canonical: awards[].value.currency
    source_fields: [contractCurrency-contratMonnaie]
    transform: default_cad
  - canonical: awards[].description
    source_fields: [tenderDescription-descriptionAppelOffres-eng, title-titre-eng]
    transform: first_present
  - canonical: awards[].items[].classification.id
    source_fields: [unspsc, gsin-nibs]
    transform: first_present
  - canonical: contracts[].period.startDate
    source_field: contractStartDate-contratDateDebut
    transform: parse_date
  - canonical: contracts[].period.endDate
    source_field: contractEndDate-dateFinContrat
    transform: parse_date
