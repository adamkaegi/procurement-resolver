This data is model-generated, exists for pipeline scaffolding only, and any metric computed from it is invalid for reporting.

No formal hand-labelled evaluation set was pursued for this project (see
`docs/SPEC.md` Part 3, "Explicitly out of scope") — this provisional set is
not a placeholder waiting to be replaced by one. It exists only to exercise
the Phase 3 and Phase 5 pipelines end to end, and any number computed from
it stays labelled PROVISIONAL and is never reported as a real metric.
